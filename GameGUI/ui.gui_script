function init(self)
	self.lives = 3
end

function on_message(self, message_id, message, sender)
	if message_id == hash("take_live") then
		self.lives = self.lives - 1
		local scorenode = gui.get_node("playerLives")
		gui.set_text(scorenode, "LIVES: " .. self.lives)
	end 
end